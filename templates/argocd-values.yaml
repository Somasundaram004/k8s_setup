global:
  logging:
    format: json
    level: info

controller:
  replicas: 2

server:
  replicas: 2
  service:
    type: ClusterIP

repoServer:
  replicas: 2

applicationSet:
  replicas: 2

configs:
  params:
    server.insecure: false