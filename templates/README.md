# Local Kubernetes templates

Run `../scripts/download-templates.sh` from the repository root to download the pinned CNI manifest into this directory. Review and commit the resulting manifest so later bootstraps use the exact same template instead of a live remote URL.